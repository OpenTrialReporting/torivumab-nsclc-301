# Generated data — reproducible via data-raw/00_run_all.R
data-raw/raw_data/
data-raw/session_info.txt

# R artefacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# renv (lock file is committed; library is not)
renv/library/
renv/staging/

# OS
.DS_Store
Thumbs.db

# Claude Code — local settings and personal memory (project-wide skills ARE committed)
.claude/settings.local.json
memory/

# QC — auto-generated timestamped reports (regenerate via qc tooling).
# Reports of record should be promoted out of reports/ into a named
# subdirectory (e.g. qc/p21-reports/2026-05-16/) and committed explicitly.
qc/reports/*/

# QC programmer's independent re-derived outputs (compared against primary;
# not committed because they're the QC programmer's working files).
qc/sdtm/
qc/adam/
qc/tfl/
